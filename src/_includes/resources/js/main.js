// Footer Date
document.getElementById('year').innerHTML = new Date().getFullYear();

import './partials/_browser-update';

// fetch('testimonials.json')
//   .then(response => response.json())
//   .then(data => {
//     var min   =   1;
//     var max   =   data.testimonial.length;
//     var rand  =   Math.random() * (max - min) + min;
//     // var id    =   data.testimonial[Math.round(rand)].id;
//     var quote =   data.testimonial[Math.round(rand)].quote;
//     var name  =   data.testimonial[Math.round(rand)].name;

//     document.getElementById("demo").innerHTML = quote + ", " + name;

//   });
